<% @item.errors.full_messages.each do |message| %>
  <p><%= message %></p>
<% end %>

<%= form_for [@city,@item] do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>
  <br>
  <%= f.label :img_url %>
  <%= f.text_field :img_url %>
  <br>
  <%= f.submit %>

<% end %>
