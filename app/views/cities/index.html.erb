<h1>All Cities</h1>

<%= link_to "(+)", new_city_path%>


  <% @cities.each do |city| %>
    <div class="city">
      <a href="/cities/<%= city.id %>">
        <%= city.name %>
      </a>
    </div>
  <% end %>
